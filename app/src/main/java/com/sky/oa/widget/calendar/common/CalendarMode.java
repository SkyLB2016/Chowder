package com.sky.oa.widget.calendar.common;

public enum CalendarMode {
    /**
     * 年视图，显示的是月份
     */
    YEAR,
    /**
     * 月视图，显示的是每月的具体日期
     */
    MONTH,
    /**
     * 周视图，显示的是每周（几日-几日）
     */
    WEEK,

    DAY
}
